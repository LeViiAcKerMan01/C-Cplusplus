#include<stdio.h>
#include<stdlib.h>

int main()
{   //constants can be declared by using const keyword before the variable datatype
    const int NUM=5; // Its a trend to initialise varible with capital letters although not necessary!
    printf("%d\n", NUM);
    return 0;
}